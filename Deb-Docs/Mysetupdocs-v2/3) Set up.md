## Set up curl
``` bash
sudo apt install curl
```
## Set up wget
``` bash
sudo apt install wget
```
## Set up git
``` bash
sudo apt install git
```
## Set up unzip
``` bash
sudo apt install unzip
```

## Set up snap if needed for microk8s
``` bash
sudo apt install snapd -y
```